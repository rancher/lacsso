import { selectProperty } from 'dummy/helpers/select-property';
import { module, test } from 'qunit';

module('Unit | Helper | select property');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectProperty([42]);
  assert.ok(result);
});
