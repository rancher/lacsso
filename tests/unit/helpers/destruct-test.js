import { destruct } from 'dummy/helpers/destruct';
import { module, test } from 'qunit';

module('Unit | Helper | destruct');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = destruct([42]);
  assert.ok(result);
});
